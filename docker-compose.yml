version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
      - "50002:50002/udp"
    restart: always

  frontend:
    build:
      context: ./front
      dockerfile: Dockerfile
    ports:
      - "12346:80"
    restart: always
